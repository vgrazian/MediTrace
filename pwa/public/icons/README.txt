# MediTrace PWA — placeholder icons
#
# Replace these with real PNG icons before going to production.
# Recommended tool: https://maskable.app/editor or vite-plugin-pwa's generateSW icons option.
#
# Required:
#   pwa-192.png  (192×192 px, RGB, PNG)
#   pwa-512.png  (512×512 px, RGB, PNG, also used as maskable)
#
# Quick generation from the favicon.svg:
#   npx sharp-cli --input ../public/favicon.svg --output pwa-192.png resize 192 192
#   npx sharp-cli --input ../public/favicon.svg --output pwa-512.png resize 512 512
